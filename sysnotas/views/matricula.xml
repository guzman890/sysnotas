<?xml version="1.0" encoding="UTF-8"?>
<openerp>
    <data>
        <!-- window action -->
        <!--
            The following tag is an action definition for a "window action",
            that is an action opening a view or a set of views
        -->
        <!-- Action to open alumno -->
        <act_window 
            id='do_matricula_action' 
            name='matricula' 
            res_model='sysnotas.matricula' 
            view_mode='tree,form'/>

        <menuitem 
            id="second_left_menu_matricula" 
            name="matriculas" 
            parent="first_left_menu" 
            action="do_matricula_action"/>
        <!-- Full id location:
             action="openacademy.course_list_action"
             It is not required when it is the same module -->

        <!-- view -->
        <record id='view_form_matricula' model='ir.ui.view'>
            <field name='name'>matricula Form</field>
            <field name='model'>sysnotas.matricula</field>
            <field name='arch' type='xml'>
            <form>
                <header>
                </header>
                <sheet>
                <group name="group_top">
                    <group name="group_left">                
                        <field name='matricula_codigo'/>
                        <field name='matricula_alumno_cui'/>
                        <field name='matricula_alumno_obs'/>
                        <field name='matricula_curso_codigo'/>

                    </group>
                </group>
                </sheet>
            </form>
            </field>
        </record>

        <!-- Tree -->
        <record id="view_tree_matricula" model="ir.ui.view">
            <field name='name'>matricula tree</field>
            <field name='model'>sysnotas.matricula</field>
            <field name='arch' type='xml'>
            <tree>
                    <field name='matricula_codigo'/>
                    <field name='matricula_alumno_cui'/>
                    <field name='matricula_curso_codigo'/>
            </tree>
            </field>
        </record>
    </data>
</openerp>